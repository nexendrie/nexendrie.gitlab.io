image: shipito/php:7.0

pages:
  stage: deploy
  environment: pages
  before_script:
  - composer install --prefer-dist
  - mkdir public
  script:
  - ./vendor/bin/generate-site
  cache:
      paths:
      - vendor
  artifacts:
      paths:
      - public
  only:
  - master
