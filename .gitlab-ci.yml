cache:
  paths:
  - vendor/

pages:
  image: nexendrie/php:8.3
  stage: deploy
  environment:
      name: pages
      url: https://nexendrie.gitlab.io
  script:
  - phing generate
  cache:
      paths:
      - vendor
  artifacts:
      paths:
      - public
  only:
  - master
