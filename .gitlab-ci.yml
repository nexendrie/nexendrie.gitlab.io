image: nexendrie/php

pages:
  stage: deploy
  environment: pages
  before_script:
  - composer install --prefer-dist
  script:
  - ./vendor/bin/generate-site
  cache:
      paths:
      - vendor
  artifacts:
      paths:
      - public
  only:
  - master
